<UserControl x:Class="StudentInsight.Views.Authentication.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid>
        <Border Width="350"
                Padding="30"
                Background="White"
                CornerRadius="10"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                BorderBrush="#E5E7EB"
                BorderThickness="1">

            <StackPanel>

                <TextBlock Text="Register"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#1E3A8A"
                           Margin="0,0,0,20"/>

                <!-- Username -->
                <TextBlock Text="Username"/>
                <TextBox Height="35"
                         Margin="0,5,0,10"
                         Text="{Binding Username}"/>

                <!-- Email -->
                <TextBlock Text="Email"/>
                <TextBox Height="35"
                         Margin="0,5,0,10"
                         Text="{Binding Email}"/>

                <!-- Password -->
                <TextBlock Text="Password"/>
                <PasswordBox x:Name="PasswordBox"
                             Height="35"
                             Margin="0,5,0,20"/>

                <!-- Register Button -->
                <Button Content="Register"
                        Height="40"
                        Background="#1E3A8A"
                        Foreground="White"
                        Command="{Binding RegisterCommand}"
                        CommandParameter="{Binding ElementName=PasswordBox}"
                        Margin="0,0,0,10"/>

                <!-- Back to Login -->
                <TextBlock HorizontalAlignment="Center">
                    <Hyperlink Command="{Binding GoToLoginCommand}">
                        Back to Login
                    </Hyperlink>
                </TextBlock>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>